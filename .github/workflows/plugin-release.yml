# Plugin Release — disabled
# Bridge plugin at plugins/daz3d-bridge/ — enable when ready for automated builds.
name: Plugin Release (disabled)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Bridge plugin at plugins/daz3d-bridge/ — enable for automated builds"
